<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <link rel='stylesheet' href='bnb_styles.css'>
    <title class=headings>Makers BnB</title>
  </head>

  <body>

    <div class="dropdown">
      <button name ='dropbtn' class="dropbtn">Menu</button>
        <div class="dropdown-content">
          <a href="/my_bookings/:user_id">Manage Bookings</a>
          <a href="/my_spaces">Manage Spaces</a>
          <form action='/sign_out' method='post'>
            <input type='submit' name='sign_out' value='Sign Out'>
          </form>
        </div>
    </div>

    <div class=content>
      <h1 class=headings>Welcome, <%=@user.name%>!</h1>
    </div>

    <h2 class=headings> Select a date: </h2>
    <!-- in value add ruby date method for current day -->
     <!-- ruby method that sets min as today and max as one year from now-->
    <form action='user/selected_date' method='post'>
      <input type="date" id="start" name="selected_date"
             value="2020-03-27"
             min="2020-03-27" max="2021-03-27">
      <input type='submit' value='Submit'>
    </form>





    <h2 class=headings>List of all spaces</h2>

    <ul>
    <%@spaces.each do |space| %>
    <li class="space">
        <%=space['name']%>
        <%=space['description']%>
        <%=space['price']%>
      <form action="/spaces/book_space/<%=space['space_id']%>" method="get">
        <input type='submit' value="Book">
      </form>
    </li>
    <%end%>
    </ul>

    <p><form action="/my_bookings/:user_id" method="get">
        <input type='submit' value="My Bookings">
    </form>

    <p><form action="/my_spaces" method="get">
        <input type='submit' name='my_spaces' value="My Spaces">
    </form>

  </body>
</html>
