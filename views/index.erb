<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title class=headings>Makers BnB</title>
    <link href="bnb_styles.css" rel="stylesheet">
  </head>

  <body>
    <h1 class=headings>Welcome to MakersBnB!</h1>
    <div class=homepage>

    <% flash.keys.each do |type| %>
      <div data-alert class="flash <%= type %> alert-box radius">
        <%= flash[type] %>
      </div>
    <% end %>

    <p class=subheadings>List of all spaces</p>
    <ul>
    <%@spaces.each do |space| %>
    <li class="space">
        <%=space['space_name']%>
        <%=space['description']%>
        <%=space['price']%>
    </li>
    <% end %>
    </ul>

    <form action="/sign_up" method="get">
        <input type='submit' value="Sign Up">
    </form>

    <form action="/sign_in" method="get">
        <input type='submit' value="Sign In">
    </form>
  </div>
  </body>
</html>
